[package]
name = "lau-vibe-compiler"
version = "0.1.0"
edition = "2021"
description = "The vibe-to-code compiler — natural language compiles deterministically to PLATO operations"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
